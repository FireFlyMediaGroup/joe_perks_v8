# Turbopack uses `turbopack.root` = monorepo root in apps; resolve deps from root node_modules.
# Without hoisting, `@sentry/nextjs` (used by `@repo/observability`) fails to resolve during dev/build.
public-hoist-pattern[]=*@sentry*
